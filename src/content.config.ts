import { defineCollection } from "astro:content";

// Colecciones de contenido. SHC Digital actualmente no publica contenido
// editorial repetible; se deja el config base para habilitar el patrón
// cuando exista un caso de uso real (YAGNI: no se crean colecciones vacías).

export const collections = {};
