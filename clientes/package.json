{
  "name": "shc-clientes-sso",
  "version": "1.0.0",
  "private": true,
  "description": "SSO de clientes SHC Digital — login + redirect a paneles admin de cada cliente.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:session": "wrangler kv namespace create SESSIONS"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}