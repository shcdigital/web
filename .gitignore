# Sistema operativo
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/

# Entorno / build (por si algún día hay tooling)
node_modules/
dist/
.astro/
.env

# Worker SSO de clientes — secretos locales y caché
clientes/.dev.vars
clientes/.wrangler/

# Worker API de briefings (Resend) — secretos locales y caché
briefing-api/.dev.vars
briefing-api/.wrangler/

# Reels (videos para redes sociales — no se suben a git)
reels/

# Cookies temporales de curl / pruebas (pueden contener sesiones)
t.txt
*.cookies.txt